# Chakra Upstream Fixture (3.34.0)

This fixture directory stores the baseline metadata used for parity denominator generation.

Current baseline:
- package: `@chakra-ui/react`
- version: `3.34.0`

Reference commands:
- `pnpm baseline:lock:verify`
- `pnpm parity:baseline:freeze`
- `pnpm parity:baseline:verify`
