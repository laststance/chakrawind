/**
 * Entry point for the compatibility harness skeleton.
 * @returns {void}
 * @example
 * runHarness()
 */
const runHarness = () => {
  console.log("compat-harness skeleton ready")
}

runHarness()
